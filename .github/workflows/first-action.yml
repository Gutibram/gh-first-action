name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    name: First Job
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello"
      - name: Print goodbye
        run: echo "Bye"
